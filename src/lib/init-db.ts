// Database initialization is handled by drizzle-kit push.
// This file is kept as a no-op for backward compatibility.
export function initializeDatabase() {
  // No-op: schema is managed by drizzle-kit
}
